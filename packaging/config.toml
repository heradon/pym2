[agent]
socket = "/run/pym2/pym2.sock"
state_dir = "/var/lib/pym2"

[agent.web]
enabled = false
host = "127.0.0.1"
port = 17877
# password = "change-me"

# Add managed apps below.
# [[apps]]
# name = "api"
# cwd = "/srv/api"
# command = ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
# env_file = "/srv/api/.env"
# venv = ".venv" # legacy mode only (if command is empty)
# entry = "app.main:app" # legacy mode only
# args = ["--host", "0.0.0.0", "--port", "8000"] # legacy mode only
# autostart = true
# restart = "on-failure"
# stop_signal = "SIGTERM"
# kill_timeout_ms = 8000
# restart_schedule = "daily@03:00"
# env = { PYTHONUNBUFFERED = "1" }
